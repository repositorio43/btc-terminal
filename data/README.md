Esta carpeta está vacía a propósito.

Corré `python scripts/build_historical_data.py` (ver README.md principal)
para generar c1.json, c2.json, c3.json y manifest.json acá adentro.

Mientras no existan, index.html funciona igual mostrando datos
ilustrativos con un aviso claro — no hace falta esto para probar el sitio,
solo para tener el histórico real de Ciclos 1-3.
